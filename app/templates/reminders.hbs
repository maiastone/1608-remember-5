<section>
  <ul>
    {{#each model as |reminder|}}
      <li>
        {{#link-to 'reminders.reminder' reminder}}
          <button class='spec-reminder-item'>{{reminder.title}}</button>
        {{/link-to}}
      </li>
    {{/each}}
  </ul>
</section>

{{#link-to 'reminders.new'}}<button
  class='new-reminder-button'>New Reminder</button>{{/link-to}}

{{outlet}}
