<div>
    <h1 class='spec-reminder-title'>{{model.title}}</h1>
    <p class='spec-reminder-date'>{{model.date}} </p>
    
    {{#if model.notes}}
      <p class='spec-reminder-note'>{{model.notes}}</p>
    {{/if}}

    {{#link-to 'reminders'}}
      Return to Reminders
    {{/link-to}}
</div>
